Agis en tant que Lead Frontend Developer. L'audit a mis en évidence un problème de layout (`max-w-xl` au lieu de `max-w-5xl`) et un design qui ne respecte pas notre charte "Soft Premium" (emojis, boutons non standardisés). 

TÂCHES STRICTES DE REFONTE (UI & LAYOUT) :

1. CORRECTION DU LAYOUT ET DU HEADER :
- Importe les icônes `Bell`, `CheckCheck` et `BellOff` depuis `lucide-react`.
- Modifie le conteneur du header pour l'aligner avec le reste de l'app : utilise `max-w-5xl mx-auto flex items-center justify-between`.
- Ajoute l'icône `Bell` à côté du titre : `<h1 className="text-2xl font-extrabold text-white flex items-center gap-2"><Bell size={24} /> Alertes</h1>` (Tu peux retirer "arrosage" du titre pour rendre la page plus globale).
- Standardise le bouton "Tout marquer lu" : 
  `<button className="flex items-center gap-2 bg-white/20 hover:bg-white/30 text-white text-sm font-medium px-4 py-2 rounded-xl transition shadow-sm" onClick={marquerLues}><CheckCheck size={18} /> Tout marquer lu</button>`

2. REFONTE DE L'EMPTY STATE (Aucune alerte) :
- Supprime IMMÉDIATEMENT l'emoji 🔔.
- Remplace le bloc par ce design B2B élégant :
  ```jsx
  <div className="flex flex-col items-center justify-center min-h-[50vh] text-center">
      <div className="w-16 h-16 bg-gray-50 flex items-center justify-center rounded-full mb-4 text-gray-400">
          <BellOff size={32} />
      </div>
      <p className="text-lg font-bold text-gray-800">Aucune alerte pour le moment</p>
      <p className="text-sm text-gray-500 mt-1">Vous êtes à jour. Les nouvelles alertes apparaîtront ici.</p>
  </div>
